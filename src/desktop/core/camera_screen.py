from PySide6.QtWidgets import QVBoxLayout, QLabel

class CameraScreen:
    def __init__(self, parent_widget):
        self.parent = parent_widget
        self.setup_ui()
        
    def setup_ui(self):
        # Здесь будет код для настройки интерфейса экрана камеры
        # Используйте элементы из self.parent (например, self.parent.cameraIPInput)
        pass